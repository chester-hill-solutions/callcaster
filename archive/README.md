# Archive

This directory stores non-runtime files moved out of the workspace root.

## Structure

- `root-legacy/` - legacy root backup files retained for reference.
- `deprecated/twilio-serverless/` - deprecated Twilio Serverless implementation files.

Everything in this directory should be treated as historical/reference material unless explicitly reactivated.
