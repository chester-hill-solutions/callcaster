# Worker service config (callcaster-worker).
# App service uses Dockerfile by default; this file pins the worker image.

[build]
dockerfilePath = "Dockerfile.worker"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
