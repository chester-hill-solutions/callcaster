#!/usr/bin/env node
/**
 * Legacy Deno edge-function typecheck — superseded by shared/ + Vitest (Track 3C/3D).
 */
console.log("check-deno-functions: skipped (client edge functions removed)");
process.exit(0);
