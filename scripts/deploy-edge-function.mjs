#!/usr/bin/env node
/** @deprecated Postgres Edge Functions removed — use Remix routes + Bun worker. */
console.error("deploy-edge-function: client edge functions removed; nothing to deploy.");
process.exit(1);
