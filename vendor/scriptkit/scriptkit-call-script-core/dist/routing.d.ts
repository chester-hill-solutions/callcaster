import type { RoutingAnswer, RoutingResult, ScriptDocument } from "./types.js";
export declare function evaluateRouting(doc: ScriptDocument, answers: RoutingAnswer[], startPageId?: string): RoutingResult;
