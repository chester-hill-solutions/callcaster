/**
 * Legacy Tailwind v3 config — unused at runtime.
 * CallCaster uses Tailwind v4 via `@tailwindcss/vite` and
 * `@chester-hill-solutions/shad-cc/theme.css` (see app/tailwind.css).
 * Kept only so older tooling that reads this path does not fail.
 */
export default {};
